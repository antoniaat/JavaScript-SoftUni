function solve(array) {
    let status = array[0];
    let town = array[1];
    let time = array[2];
    let flightNumber = array[3];
    let gateNumber = array[4];

    console.log(`${status}: Destination - ${town}, Flight - ${flightNumber}, Time - ${time}, Gate - ${gateNumber}`);
}